# Backend API URL (frontend will connect to this)
VITE_API_URL=http://localhost:3001

# Dev-only career page fetch proxy (no longer needed with full backend, kept for compatibility)
# VITE_CAREER_FETCH_URL=

# Disable public CORS relays in static deployments
# VITE_DISABLE_PUBLIC_CORS_PROXY=false
